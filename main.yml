---
- name: Install common tools
  import_playbook: install_common_tools.yml

- name: Install R build dependencies
  import_playbook: install_r_build_deps.yml

- name: Install R package builds dependencies
  import_playbook: install_r_pkg_deps.yml

- name: Install R4Pi tools
  import_playbook: install_r4pi_tools.yml

- name: Additional install and config for the build primary
  import_playbook: install_build_primary.yml
